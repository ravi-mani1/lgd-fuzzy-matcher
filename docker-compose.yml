version: "3.9"

services:
  ui:
    build: .
    ports:
      - "8501:8501"
    env_file: .env
    environment:
      - LGD_API_URL=http://api:8000

  api:
    build: .
    command: ["uvicorn", "api:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
    env_file: .env
